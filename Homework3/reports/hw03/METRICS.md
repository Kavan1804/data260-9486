# HW3 Metrics - Chunking Technique Comparison

Generated by `python -m rag.compute_metrics` from `raw/token.jsonl`,
`raw/semantic.jsonl`, and `raw/sentence_window.jsonl` (produced by
`python -m rag.run_experiments` against the 5 questions in `questions.yaml`
and the corpus described in `SOURCES.md`/`CORPUS_MANIFEST.json`). Full table
source: `raw/summary_metrics.json`.

| Technique | Chunks | Avg chunk length | Top-1 cosine | Mean@k cosine | Recall@k | Mean retrieval latency (ms) |
|---|---:|---:|---:|---:|---:|---:|
| Token | 2365 | 307.1 | 0.5949 | 0.5158 | 1.00 | 53.91 |
| Semantic | 18 | 36072.1 | 0.3344 | 0.1951 | 1.00 | 15.33 |
| Sentence window | 236 | 2751.3 | 0.5819 | 0.4867 | 0.80 | 14.03 |

**Known data issue:** `hud_fair_market_rents.txt` downloaded as a 0-byte file
(see `rag/corpus_prep.py` console output / `CORPUS_MANIFEST.json`), so `q4`
in `questions.yaml` has no real source content backing it. This should be
re-downloaded from a working HUD FMR URL and the experiment re-run before
these numbers are treated as final; the recall@k values above are measured
against the corpus as currently downloaded, including this gap.

## Confidently scored miss

From `raw/summary_metrics.json` -> `confident_misses` (7 total found across
techniques, cosine >= 0.5 threshold, chunk text does not contain the
expected answer). Clearest example:

- **Technique:** Token
- **Query (q3):** "Which federal law prohibits housing discrimination based on race, color, religion, sex, national origin, familial status, and disability?"
- **Expected answer:** "The Fair Housing Act of 1968"
- **Cosine similarity:** 0.6440
- **Retrieved chunk preview:** "Credit Opportunity Act</a> have alleged discrimination based on race or color. Some of the Department's cases have also alleged that municipalities and other lo..."

**Why the embedding likely scored this high:** the retrieved chunk is from
the same DOJ Fair Housing Act page but discusses a *related* law (the Equal
Credit Opportunity Act) in a passage about race/color discrimination claims.
It shares heavy topical and lexical overlap with the query (discrimination,
race, color, federal enforcement) without actually naming the Fair Housing
Act, so the embedding model scores it as similar based on shared vocabulary
and subject matter rather than shared facts.

The other 6 confident misses (q1, q2, q5 under token; q3 under semantic and
sentence_window) show the same pattern: adjacent statutory text on the same
topic scoring highly without containing the literal answer. Full list in
`raw/summary_metrics.json`.

## AI use

See `AI_USE.md` for the four required AI-use questions.
